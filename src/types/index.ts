// ──────────────────────────────────────────────
// Shared types
// ──────────────────────────────────────────────
// Types that are used across multiple features.
// Feature-specific types live in their own
// features/<name>/types.ts file.
// ──────────────────────────────────────────────

// Generic section props — currently unused but
// ready for when sections need shared props
export interface SectionProps {
  className?: string
}
